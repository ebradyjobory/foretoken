<% @futures.each do |future| %>

<%= future.future_year %>
<%= link_to("Edit", {:action => 'edit', :id => future.id, :project_id => @project.id }) %>
<%= link_to("Delete", {:action => 'delete', :id => future.id, :project_id => @project.id}) %>

<%end%>

<%= link_to("New", {:action => 'new', :project_id => @project.id }) %>

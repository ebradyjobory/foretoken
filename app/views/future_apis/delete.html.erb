
<div>
  <h2>Delete Page</h2>

  <%= form_for(:future, :url => {:action => 'destroy', :id => @future.id, :project_id => @project.id }) do |f| %>

    <p>Are you sure you want to permanently delete this page?</p>
    <div>
      <%= submit_tag("Delete Page", :class => "btn btn-primary") %>
    </div>

  <% end %>
</div>
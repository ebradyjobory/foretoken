<%= form_for(:future, :url => { :action => 'create', :project_id => @project.id }) do |f| %>

	<%= render(:partial => 'form', :locals => {:f => f }) %>
	<br>
	<%= submit_tag('Create New Data', :class => "btn btn-primary") %>
<%end%>
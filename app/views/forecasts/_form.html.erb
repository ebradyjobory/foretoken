<%= error_messages_for(@forecast) %>
	
	<%= form_for [@project, @forecast], :remote => true do |f| %> 
	  <%= f.text_field :year %>
	  <%= f.text_field :value %>
	  <%= f.submit "Save", :class => "" %>
	<%end%>
